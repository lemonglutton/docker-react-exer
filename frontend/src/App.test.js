import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // this is testing always, since we don't have any specific rules for this simple app
});
